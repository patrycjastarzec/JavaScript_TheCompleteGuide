/* 1. Create two variables: one that holds a fictional user input (a number of your choice) and one that holds no value initially (e.g. "result") */

let userInputVariable = 5;

let result;

/* 2. Set the result value to 18 plus the value stored in user input  */

result = 18 + userInputVariable;

/*Add three additional lines of code where you change the result variable again (by substracting a value of your choice, multiplying and dividing it) */

result = result + 1000 / 2 + 25 - 11;
result = result * 2;
result = (result / 4) + 25;

/*Did the value of user input changed?*/

/* My answer - no */

/* alert() the result and the user input variables (in two seperate aler code) */

alert(result);
alert(userInputVariable);